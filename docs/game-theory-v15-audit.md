# v15.0.1 实际缺口核验与验收

本次只核验并补齐v15既有范围，未扩大博弈概念或真实费用预算。旧v15.0.0冻结包保留。

| 已复现的问题 | 修复 | 验收证据 |
|---|---|---|
| 学习设2999轮，曲线末尾却为2987轮 | 无论采样间隔都记录最后一轮 | 4博弈×2算法×3轮数×2种子，共48组99216轮，最终频率和遗憾与最终诊断全部一致 |
| 历史界面只显示30条，服务最多返回100条 | API分页和界面「加载更早的实验」 | 隔离目录105条记录完整遍历；本机58条列表实际加载第30条之后的旧混合均衡实验 |
| 单条损坏记录使全部历史无法读取 | 单条校验隔离、明确提示、保留原文件 | 105条中损坏2条，其余103条分页无丢失、无重复，损坏项单独返回422 |
| 布尔值被当作数值，部分整数参数缺乏明确边界 | 严格有限数值及整数计数校验 | 布尔、非有限、字符串、空值、非整数计数被拒绝，不生成记录 |
| 历史请求省略参数时复跑沿用后来表单状态 | 按原API默认参数补齐后再应用显式输入 | 前端默认参数/覆盖值恢复测试通过 |
| 帕累托标记和公共品收益标签范围不够明确 | 明确只比较四个纯动作结果，资源显示为期末余额 | 不声称检验了全部随机化组合的帕累托最优 |

## 补充实验结论

48组学习实验使用预先固定的201、2999、3001轮与两个种子，覆盖两算法和四情境。末尾的行动频率、外部遗憾与经验联合分布诊断一致；这修正了证据展示，不代表策略收益提高。

五种市场策略加规则基线各重跑20轮，共6局120轮，使用历史共同种子330101，全部重放通过且最终状态hash与原v15逐一一致。因此原利润/福利方向结论没有因本次修补改变。没有添加新的策略晋级结论。

原6次真实模型原始响应逐一核对，已选择选项及偏离损失与验收记录一致。本轮新增付费调用0次，累计保守预留9.993565元、剩余0.006435元，未修改原10元上限。

实际执行新备份、停止、恢复、启动。恢复前后58条概念实验、8个已完成任务、1个普通存档和504个JSON文件的API内容/哈希一致，预算账未回滚。

产物：runs/theory-v15-audit/preregistration.json、learning.json、summary.json、backup-restore.json；独立修订包releases/local-market-v15.0.1。复现实验脚本scripts/run_theory_v15_audit.py不接入模型客户端。

最终回归：455项后端测试通过（0失败/错误/跳过），前端12项测试、生产构建、类型检查和lint通过。历史按钮新增情境/算法/轮数/种子与记录编号摘要，避免同秒多条实验无法区分；加载后定位到结果。
